apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloudops-backend
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: cloudops-backend
  namespaceSelector:
    matchNames:
      - cloudops
  endpoints:
    - port: http
      path: /metrics
      interval: 15s