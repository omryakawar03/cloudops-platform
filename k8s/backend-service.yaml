apiVersion: v1
kind: Service
metadata:
  name: cloudops-backend
  namespace: cloudops
  labels:
    app: cloudops-backend
spec:
  type: ClusterIP
  selector:
    app: cloudops-backend
  ports:
    - name: http
      port: 3001
      targetPort: http